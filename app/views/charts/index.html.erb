<div class="row">
  <!-- Line Chart - Single Series -->
  <div class="col-xs-6">
    <h3>Charts</h3>
    <%= @place2year_price.length %><br>
    <%= @place2year_price.class %><br>
    <% @place2year_price.each do |key, value| %>
    <%= key %><br>
    <%= value %><br>
    <% end %>
  </div>
</div>
